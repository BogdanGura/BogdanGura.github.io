export class Player
{
    //Constructor for player
    constructor(name, winMessage, loseMessage)
    {
        this.name = name;
        this.winMessage = winMessage;
        this.loseMessage = loseMessage;
    }
}